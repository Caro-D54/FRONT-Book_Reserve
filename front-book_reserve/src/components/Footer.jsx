import React from "react";

const Footer = () => (
    <footer className="bg-light mt-auto py-3">
        <div className="container text-center">
            <small>&copy; {new Date().getFullYear()} Biblioteca Nexus Literario</small>
        </div>
    </footer>
);

export default Footer;